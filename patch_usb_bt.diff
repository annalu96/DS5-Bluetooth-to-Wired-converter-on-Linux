<<<<<<< SEARCH
// Callback when Bluetooth receives data
void on_bt_data(CHANNEL_TYPE channel, uint8_t *data, uint16_t len) {
    // Ignore control channel for now in main mapping
    if (channel != INTERRUPT) return;
=======
#include <map>
#include <vector>

std::map<uint8_t, std::vector<uint8_t>> feature_data;

// Callback when Bluetooth receives data
void on_bt_data(CHANNEL_TYPE channel, uint8_t *data, uint16_t len) {
    if (channel == CONTROL) {
        if (len > 1 && data[0] == 0xA3) {
            uint8_t report_id = data[1];
            // Subtract 1 for A3, and assume the last 4 bytes are CRC or just copy the payload
            // A3 [ID] [DATA...] [CRC 4 bytes]
            // We'll store what old_bt.cpp did: packet+1 to packet+size (which included CRC in old_bt, but wait, old_bt was:
            // feature_data[report_id].assign(packet + 1, packet + size);
            // which stored ID + DATA + CRC.
            feature_data[report_id].assign(data + 1, data + len);
            // printf("[BT] Stored Feature Report 0x%02X, len=%u\n", report_id, len - 1);
        }
        return;
    }

    if (channel != INTERRUPT) return;
>>>>>>> REPLACE
