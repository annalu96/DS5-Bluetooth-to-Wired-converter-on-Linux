Plan for completing Phase 5:

1. Create a Python script (`build_desc.py`) that will update `src/usb.cpp` with a new `descriptors` struct containing the complete USB audio and HID configuration. We will extract the exact descriptor blocks from `old_usb_conf_extracted.c`, starting from the first Interface descriptor, and compute the correct descriptor count (26). Wait, the `fs_count` includes interface, endpoint, class specific descriptors... all of them.
   - Set `fs_count` and `hs_count` to 26.
   - Insert the raw hex array as `fs_desc` and `hs_desc`.
   - Update `descriptors.fs_desc[199]` or whatever the wDescriptorLength offset is to `desc_hid_report_ds_len` inside `usb_init`. Actually, `wDescriptorLength` is 2 bytes at offset 209 in `old_usb_descriptors.c` (since the HID report desc is dynamically sized). We can calculate the exact index for it.

2. Modify file descriptor variables in `src/usb.h` and `src/usb.cpp`:
   - Keep `ep0_fd`.
   - Rename `ep1_fd` to `ep_audio_out_fd`.
   - Rename `ep2_fd` to `ep_audio_in_fd`.
   - Rename `ep3_fd` to `ep_hid_in_fd`.
   - Rename `ep4_fd` to `ep_hid_out_fd` (and declare it).

3. In `usb_init` (`src/usb.cpp`):
   - Open `/dev/ffs/ep1` -> `ep_audio_out_fd`.
   - Open `/dev/ffs/ep2` -> `ep_audio_in_fd`.
   - Open `/dev/ffs/ep3` -> `ep_hid_in_fd`.
   - Open `/dev/ffs/ep4` -> `ep_hid_out_fd`.

4. In `src/main.cpp`:
   - Replace `ep1_fd` with `ep_hid_in_fd` in the `on_bt_data` BT callback.
   - Replace `ep2_fd` with `ep_hid_out_fd` in the epoll logic where it reads rumble data from Proton.
   - Add `ep_audio_out_fd` to `epoll` using `EPOLLIN`.
   - When `ep_audio_out_fd` has `EPOLLIN`, read the raw PCM data (up to 392 bytes, which is `wMaxPacketSize` of EP1) and pass it to `audio_receive_pcm((const int16_t*)buf, ret)`.

5. Ensure all `write` and `read` occurrences map to the new variable names.

6. Commit the plan and verify using Pre-Commit check.
